// state "blocks"
// accept input and produce output
// both must be objects
//
// Blocks form a DAG, and at the end you can submit (really at any point)

// builder pattern to build state

// create zustand store to put the data in, and make provider for the store

// In theory we can support loops/etc, maybe forms don't have to be a DAG.

/*
const api: any = {};

function exampleCode() {
  const FormPage = api.form({
    fields:  ''
  });


    function ExampleComponent() {


    }
}
*/
